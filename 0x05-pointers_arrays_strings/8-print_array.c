#include "main.h"

/**
 * print_array - a function that prints n elements of an array
 * @a: is an argument
 * @n: is an argument
 * Return: void
 */

void print_array(int *a, int n)
{
	int indexArr;

	indexArr - 0;
	while (n > 0)
	{
		printf("%d", a[indexArr];
		n--;
		if (n > 0)
		{
			printf(", ");
		}
		indexArr++;

	}
	printf("\n");

}
