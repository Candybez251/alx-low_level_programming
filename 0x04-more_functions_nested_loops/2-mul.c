#include "main.h"

/**
 * mul - a function to multiply two integers
 * @a: a variable
 * @b: a variable
 * Return: multiple
 */

int mul(int a, int b)
{
	int multiply;

	multiply = a * b;
	return (multiply);
}
